# M1 一期开发完成

> 类型：里程碑 · 日期：2026-08-29

- 技术栈落地：electron-vite + React 18 + TypeScript + better-sqlite3（Electron 33.4.11）。
- 脚本模块：CRUD/分组/扫描勾选登记/一键执行（zsh 或 bash、进程树停止、日志面板、历史 5 条/一键清理、执行前确认开关）。
- 项目模块：CRUD/多根目录 git 扫描/README 草稿/技术栈猜测/状态筛选与归档折叠/打开文件夹·终端·VS Code·复制路径/Git 信息（分支·最后提交·未提交改动）。
- 通用：单窗口三页签；搜索；深浅主题；卡片/列表；设置页；JSON 备份导出与导入恢复（导入前自动备份到 userData/backups）。
- 质量门：typecheck、eslint、build 全部通过；预览与打包应用启动冒烟通过。
- 打包：electron-builder 双架构（arm64/x64）→ `release/mac-arm64`、`release/mac`；未签名；图标暂为 Electron 默认（列入 M3）。
- 数据目录：`~/Library/Application Support/Workbench/workbench.db`。
- 待办：按 Test.md 手工验收清单逐项测试；如需可申请免费 Apple ID 签名。
