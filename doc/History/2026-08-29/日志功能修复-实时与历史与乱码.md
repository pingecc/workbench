# 日志功能修复：实时日志 / 历史完整 / 乱码

> 类型：缺陷修复 · 日期：2026-08-29

- 点击「运行」后自动打开日志面板，实时逐行输出（端到端验证：status + 逐行 log 事件链路正常）。
- 历史日志：单次运行输出上限从 200KB 提升到 5MB / 10 万行（日常脚本基本等于完整保存）；运行结束后历史列表自动刷新。
- 乱码修复：捕获时剥离 ANSI 颜色控制符、其他转义序列与 `\r`，彩色输出不再出现乱码（清理函数已验证：彩色中文文本正常保留）。
- 历史仍按需求保留最近 5 次运行（如需更多可再调）。
- 重新打包 release .app（arm64 / x64）。
