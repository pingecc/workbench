# 第 2 轮决议

> 类型：需求决议 · 日期：2026-08-29

- 脚本形态：以 macOS 原生 `.sh` / `.command` 为主；运行展示 = 应用内日志面板；支持分组；历史仅保留最近 5 次、可一键清理；单脚本“执行前确认”开关（默认关）。
- 项目：默认扫描根目录 `~/pinge-system/code`；编辑器 = VS Code；终端 = macOS 系统 Terminal；不需要“最近打开时间”排序；项目快捷命令改二期。
- 前端技术栈：React + TypeScript。
- 数据：Electron 默认数据目录；导出 JSON 备份 + 导入恢复。
