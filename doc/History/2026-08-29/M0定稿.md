# M0 定稿（收尾确认 Q30-Q35 全部通过）

> 类型：里程碑 · 日期：2026-08-29

- 执行 Shell：默认 `zsh`，单脚本可切 `bash`。
- 导入恢复：导入前自动备份当前数据，再覆盖导入。
- M1 任务拆分通过：14 项任务、约 13 天（Task.md）。
- 打包签名：一期自用未签名 `.app`（首次“右键 → 打开”）；不做自动更新。
- 已知限制：PRD 第 6 节通过评审。
