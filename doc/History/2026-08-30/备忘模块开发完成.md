# 备忘模块开发完成

> 类型：功能迭代 · 日期：2026-08-30

- DB 迁移 v3：新增 `notes` 表（`type` / `title` / `body` / `done` / `due_date` / `priority` / `created_at` / `updated_at` / `completed_at`）+ 类型与完成状态索引；老库启动时自动升级。
- 主进程：新增 `src/main/services/notes.ts`（增删改查，排序「未完成优先 + 创建时间倒序」，完成时写 `completed_at`）；IPC 新增 `notes:list`、`note:create/update/delete`；preload 暴露对应受限 API。
- 备份：导出/导入同步 `notes` 数据；`BackupSummary` 新增 `notes` 计数；导入旧版备份文件时按空数组容忍。
- 渲染层：新增 `NotesPage.tsx` 与侧边栏「备忘 📝」页签（脚本/项目之后）；列表分区（待办 / 已完成折叠 / 想法）、类型与关键词筛选、勾选完成/取消完成（已完成划线并折叠）、新建/编辑弹窗（类型切换时待办字段随类型显示/隐藏）、三态空状态；侧边栏计数同步。
- 界面：样式沿用现有设计令牌（浅/深主题、chips、徽章、弹窗、空状态），新增 `.note-*` / `.pri-*` / `.due-badge` / `.type-seg` 等类。
- 原型：`prototype/notes-prototype.html` 已按确认方案转为真实功能实现。
- 验证：`npm run typecheck`、`npx eslint src --max-warnings 0`、`npm run build` 全部通过；内存库冒烟验证 v3 迁移 SQL、排序、`completed_at`、类型字段语义正确；手工验收清单见 `doc/Test.md` 备忘模块与 H7-H9。
