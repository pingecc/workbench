# 备忘模块：待办排序调整

> 类型：功能调整 · 日期：2026-08-30

- 原方案（原型确认时）：待办未完成优先 + 创建时间倒序。
- 调整（用户反馈）：待办事项改为**按优先级排序**——高 → 中 → 低；相同优先级再按创建时间倒序。
- 实现：`NotesPage.tsx` 新增 `PRIORITY_RANK` 与 `byPriorityThenCreated` 排序函数，待办（未完成与已完成折叠区）统一按「优先级 + 创建时间」排序；想法保持创建时间倒序。
- 文档同步：PRD 3.4 与验收标准、doc/README.md 决议记录、Plan M4 范围、Test 验收清单（新增用例 H10）、根 README。
- 验证：typecheck / eslint / build 通过。
